<div class="card mb-3 bg-white text-dark">
  <h5 class="card-title mx-3 my-3"><strong>Mes disponibilités</strong></h5>
  <% if @lesson.user.has_schedules? %>
    <div class="card-body">
      <p>Tu as d'autres créneaux disponibles pour enseigner ce cours ? Mets vite à jour ton planning de disponibilités 😉</p>
      <div class="text-center mt-4">
        <%= link_to "Mettre à jour mon planning", user_schedules_path(current_user.username), class: "btn btn-outline-primary" %>
      </div>
    </div>
  <% else %>
    <div class="card-body">
      <p>Mets vite à jour ton planning de disponibilités, afin que des membres puissent réserver ton super cours 😇 </p>
      <div class="text-center mt-4">
        <%= link_to "Créer mon planning", user_schedules_path(current_user.username), class: "btn btn-outline-primary" %>
      </div>
    </div>
  <% end %>
</div>