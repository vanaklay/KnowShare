<div class="card mb-3 bg-white text-dark">
  <h3 class="card-header bg-white text-center"><%= @lesson.title %></h3>
  <% if @lesson.picture.attached? %>
    <%= image_tag @lesson.picture, class:"img-fluid img-lesson" %>
  <% else %>
    <%= image_tag  "https://cdn.pixabay.com/photo/2020/11/04/10/45/school-5711987_1280.jpg", class:"img-fluid img-lesson" %>
  <% end %>
  <div class="card-body">
    <h4 class="card-text">Découvrir ce cours en détail</h4>
    <hr>
    <p class="card-text"><%= @lesson.description %></p>
  </div>
  <div class="card-body">
  <hr>
    <% if @lesson.bookings.count == 1%>
      <h6 class="card-title text-muted">1 personne a suivi ce cours !</h6>
    <% elsif @lesson.bookings.count < 1 %>
      <h6 class="card-title text-muted">Soit le premier à réserver ce cours !</h6>
    <% else %>
      <h6 class="card-title text-muted"><%= @lesson.bookings.count %> personnes ont suivi ce cours !</h6>
    <% end %>
  </div>
</div>