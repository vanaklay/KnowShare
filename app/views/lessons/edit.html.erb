<div class="bg-new-class">
  <div class="container d-flex align-items-center">
    <div class="col">
      <div class="row justify-content-center mt-5">
        <div class="col-md-8 col-lg-6">
          <%= form_with model: @lesson, class: "form", id: "contact-form" do |f| %>
            <div class="card p-4 mb-5 bg-white text-dark">
              <div class="text-center mb-5">
                <h3>Édite ton cours</h3>
                <h5><%= @lesson.title %></h5>
                <p class="text-muted">Astuce : une belle description et une belle image te permettra d'attirer plus d'élèves et donc d'obtenir plus de crédits. Prêtes-y attention 😘</p>
              </div>
              <span class="clearfix"></span>

              <div class="form-group">
                <label class="form-control-label"><strong>Titre du cours</strong></label>
                <div class="input-group input-group-transparent">
                  <%= f.text_field :title, class: "form-control", placeholder: "Le point de bâti en couture", required: true, minlength: "10" %>
                </div>
              </div>

              <div class="form-group">
                <label class="form-control-label"><strong>Description</strong></label>
                <div class="input-group input-group-transparent">
                  <%= f.text_area :description, class: "form-control", rows: "7", placeholder: "Ce point sert à assembler rapidement des pièces de tissu de façon temporaire avant de faire la couture définitive.", required: true, minlength: "20", maxlength: "5000" %>
                </div>
              </div>

              <div class="form-group">
                <label class="form-control-label"><strong>Ajouter une image</strong></label>
                <div class="input-group input-group-transparent">
                  <br>
                  <%= f.file_field :picture, accept: 'image/png,image/gif,image/jpeg'%>
                </div>
              </div>

              <div class="mt-3">
                <div class="d-flex justify-content-center">
                  <%= f.submit "Publier mon cours", class: "btn btn-warning d-flex mx-auto mb-3" %>
                </div>
                <div class="d-flex justify-content-between align-items-center">
                  <button type="button" class="btn btn-secondary btn-icon">
                    <span class="btn-inner--icon"><i class="fas fa-arrow-left"></i></span>
                    <span class="btn-inner--text"><%= link_to "Page d'accueil", root_path %></span>
                  </button>
                </div>
              </div>
            </div>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
