<div class="bg-white">
  <div class="container my-1 pb-3">
    <div class="row">
      <div class="mx-2 my-2" style="position: absolute; right: 0;">
        <%= link_to "Retour vers page profil", user_path(current_user), class: "btn btn-outline-primary d-flex mx-auto mb-3" %>
      </div>
      <div class="col-md-6 offset-md-3 text-dark">
        <br><br><br>
        <%= form_for(resource, as: resource_name, url: registration_path(resource_name), html: { method: :put }) do |f| %>
          <%= render "users/shared/error_messages", resource: resource %>
          
          <h1 class="h3 mb-5 font-weight-normal text-center">Edite tes informations de connexion</h1>    

          <div class="form-group">
            <%= f.text_field :username, autofocus: true, class: "form-control", placeholder: "Pseudonyme",autocomplete: "username", required: true %>
          </div>

          <div class="form-group">
            <%= f.text_field :email, class: "form-control", placeholder: "Email",autocomplete: "email", required: true %>
          </div>

          <% if devise_mapping.confirmable? && resource.pending_reconfirmation? %>
            <div>Vous n'avez pas encore confirmé votre compte. Un email vous a été envoyé à : <%= resource.unconfirmed_email %></div>
          <% end %>

          <div class="form-group">
            <%= f.password_field :password, autocomplete: "new-password", class: "form-control", placeholder: "Nouveau mot de passe (6 caractères minumum)" %>
          </div>

          <div class="form-group">
            <%= f.password_field :password_confirmation, autocomplete: "new-password", class: "form-control", placeholder: "Confirmer le nouveau mot de passe" %>
          </div>

          <div class="form-group">
            <%= f.password_field :current_password, autocomplete: "current-password", class: "form-control", placeholder: "Mot de passe actuel", required: true %>
          </div>

          <div class="actions mt-4">
            <%= f.submit "Mettre à jour", class: "btn btn-outline-primary d-flex mx-auto mb-1" %>
          </div>
        <% end %>
        <%= button_to "Supprimer mon compte", registration_path(resource_name), data: { confirm: "Es-tu sûr(e) ?" }, method: :delete, class: "btn btn-danger d-flex mx-auto mb-3" %>
      </div>
    </div>
  </div>
</div>
